name: Lint code & compile test

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  test:
    uses: nezuchan/workflows/.github/workflows/lint-and-compile-test.yml@f1fd8acd1eb6ee76f944ab700ce7beafec5519c0 # main
    